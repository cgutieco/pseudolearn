{
  "id": "CON-B8-E1",
  "title": "Count passing grades",
  "statement": "Read a count of grades and then that many integers, one at a time, and write how many are 5 or more.",
  "level": 1,
  "kind": "predecir",
  "module": "CON-B8",
  "visibleCases": [
    {"inputs": ["3", "4", "6", "8"], "expectedOutputs": ["2"], "expected": "numerico"},
    {"inputs": ["2", "3", "4"], "expectedOutputs": ["0"], "expected": "numerico"}
  ],
  "hiddenCases": [
    {"inputs": ["5", "5", "5", "5", "5", "5"], "expectedOutputs": ["5"], "expected": "numerico"},
    {"inputs": ["1", "10"], "expectedOutputs": ["1"], "expected": "numerico"},
    {"inputs": ["4", "1", "2", "3", "4"], "expectedOutputs": ["0"], "expected": "numerico"}
  ],
  "referenceSolution": "algorithm CountPassing\n    define count as integer;\n    define i as integer;\n    define grade as integer;\n    define passing as integer;\n    read count;\n    passing <- 0;\n    for i <- 1 to count step 1 do\n        read grade;\n        if grade >= 5 then\n            passing <- passing + 1;\n        endIf\n    endFor\n    write passing;\nendAlgorithm\n"
}
