{
  "id": "CON-C4-E1",
  "title": "Shape hierarchy with overridden Area",
  "statement": "Define a Shape class with an Area method that returns 0.0, and a Square class that extends Shape, with a constructor that takes a side and overrides Area to return the square's area. Read the side, create a Square and write its area.",
  "level": 1,
  "kind": "crear",
  "module": "CON-C4",
  "visibleCases": [
    {"inputs": ["4"], "expectedOutputs": ["16.0"], "expected": "numerico"},
    {"inputs": ["0"], "expectedOutputs": ["0.0"], "expected": "numerico"}
  ],
  "hiddenCases": [
    {"inputs": ["5"], "expectedOutputs": ["25.0"], "expected": "numerico"},
    {"inputs": ["10"], "expectedOutputs": ["100.0"], "expected": "numerico"},
    {"inputs": ["3"], "expectedOutputs": ["9.0"], "expected": "numerico"}
  ],
  "assertions": [
    {"kind": "contiene", "construct": "clase", "requirement": "The statement asks you to solve it with a class."}
  ],
  "referenceSolution": "class Shape\n    public method Area() as real\n        return 0.0;\n    endMethod\nendClass\n\nclass Square extends Shape\n    private define side as integer;\n\n    method constructor(aSide as integer)\n        this.side <- aSide;\n    endMethod\n\n    public method Area() as real\n        return this.side * this.side;\n    endMethod\nendClass\n\nalgorithm ComputeSquareArea\n    define side as integer;\n    define square as Square;\n    read side;\n    square <- new Square(side);\n    write square.Area();\nendAlgorithm\n"
}
