algorithm DayOfWeek
    define day as integer;
    read day;
    switch day do
        1, 7:
            write "Weekend";
        2, 3, 4, 5, 6:
            write "Weekday";
        otherwise:
            write "Invalid day";
    endSwitch
endAlgorithm
