Class Point
  Public Define cx As Integer
  Public Define cy As Integer
  Method Constructor(px As Integer, py As Integer)
    This.cx <- px; This.cy <- py
  EndMethod
EndClass
Algorithm ClassConstructor
  Define p As Point
  p <- New Point(2, 3)
  Write p.cx + p.cy
EndAlgorithm
